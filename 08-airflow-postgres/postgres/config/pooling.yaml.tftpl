apiVersion: stackgres.io/v1
kind: SGPoolingConfig
metadata:
  namespace: ${namespace}
  name: airflowpoolconfig
spec:
  pgBouncer:
    pgbouncer.ini:
      pgbouncer:
        pool_mode: transaction
        max_client_conn: '1000'
        default_pool_size: '100'